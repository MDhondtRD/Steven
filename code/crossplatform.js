
var Switch = require('./switch');

var CrossPlatform = React.createClass({

  render: function() {
    return (
      <View>

        <Switch onValueChange={//do something}/>
      </View>
    );
  }
});
